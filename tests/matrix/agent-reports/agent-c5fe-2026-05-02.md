# Agent report — agent-c5fe-2026-05-02

**Session**: agent-c5fe-2026-05-02
**Branch**: agent/agent-c5fe-2026-05-02 (worktree
`.claude/worktrees/agent-c5fe-2026-05-02/`)
**Window**: started 2026-05-02 ~02:35 local; last on-VM iteration
2026-05-02 03:02; VM SSH dropped ~03:18 and didn't recover within
the working window. Real-world wall ~1h on-VM + ~30m offline finalising.

## Scenarios touched (23 total)

| status                                                            | count | scenarios |
|-------------------------------------------------------------------|------:|-----------|
| `passed-<session>`                                                | 1     | mac-format-mac-enumerate-empty |
| `failed-needs-iter14-sd-<session>`                                | 17    | mac-format-{basic-256mib, tiny-32mib, small-64mib, large-1gib}; mac-format-cluster-{512, 1k, 8k, 64k}; mac-format-label-{empty, 32chars, cjk, latin1}; mac-format-win-write-{tiny, medium, large, many-small, then-delete} |
| `blocked-on-mac-enumerate-cli-<session>`                          | 4     | mac-format-mac-enumerate-empty *(superseded → passed)*, mac-format-win-write-mac-enumerate, mac-format-win-write-win-delete-mac-verify-empty, win-format-win-write-mac-verify |
| `blocked-on-mac-writer-<session>`                                 | 1     | win-format-win-write-mac-write-win-verify |
| `blocked-on-mac-deleter-<session>`                                | 1     | win-format-win-write-mac-delete-win-verify |

(`mac-format-mac-enumerate-empty` was claimed/blocked then run pure-Mac and passed; final state is `passed-`.)

## Iterations performed (numbers from chkdsk-findings.md)

- **iter13**: Root rec 5's `$INDEX_ROOT` was empty, leaving every
  system record orphaned. Added `build_root_index_root_attr` +
  `RootIndexChild`; sorted under NTFS `COLLATION_FILE_NAME` (ASCII
  uppercase suffices for system names); 12 entries (`$AttrDef`,
  `$BadClus`, `$Bitmap`, `$Boot`, `$Extend`, `$LogFile`, `$MFT`,
  `$MFTMirr`, `$Secure`, `$UpCase`, `$Volume`, `.` self-link).
  $FILE_NAME copies in entries zero `allocated_size` /
  `real_size` / `file_attributes` to mirror reference's pattern.

  **Verified on-VM** (diag dirs `iter-20260502-024129` →
  `iter-20260502-030255`): the 12 "Detected orphaned file …" Stage 2
  messages are gone; chkdsk now reports `Stage 1 / Stage 2: Index
  verification completed` clean across every variant. Post-Stage-2
  reconnect-scan still errors with `An unspecified error occurred
  (frs.cxx:60f)` — same root cause across all 17 variants.

- **iter14 (UNVERIFIED — VM dropped SSH auth before re-run)**:
  embedded a 72-byte default `$SECURITY_DESCRIPTOR` on root rec 5
  (revision 1, SE_SELF_RELATIVE | SE_DACL_PRESENT, owner+group =
  S-1-5-18 NT AUTHORITY\SYSTEM, DACL grants S-1-1-0 Everyone full
  access). Reference rec 5 carries 248 bytes of self-relative SD with
  machine-specific SIDs; ours has none. Hypothesised cause of the
  iter13 post-Stage-2 frs.cxx:60f. Linux test contract holds (6/6).

  The next on-VM iteration will tell us whether (a) frs.cxx:60f
  disappears (→ matrix re-run), (b) a different chkdsk error
  surfaces, or (c) our SD layout is wrong and frs.cxx:60f persists.

## Infrastructure additions

- `scripts/test-windows-local.sh`: forwards `VOL_MB / WRAP_MB /
  CLUSTER_SIZE / LABEL` through to the runner.
- `scripts/run-windows-test.ps1`: gains `-ClusterSize` (passed to both
  `mkfs_ntfs -c` and `format.com /A:` so the byte-diff is
  apples-to-apples) and a `Get-ChildItem` enumerate-after-mount step
  for the `win:enumerate(empty)` operation leg.
- `scripts/reset-non-passed.sh`: pass-2/3 helper that resets every
  non-`passed-*` scenario to `pending`.
- `src/bin/inspect_ntfs.rs`: thin CLI wrapping `Filesystem::{mount,
  read_dir, stat, read_file, create_file, mkdir, unlink, rmdir}` with
  a stable text output the matrix runner can grep. Unblocks the
  mac-side `enumerate / write / delete` legs.

## Notable for the morning review

1. **iter14 is committed but not chkdsk-verified.** First action:
   `bash scripts/test-windows-local.sh` against the default scenario
   parameters (256 MiB / 4096 cluster / "CITEST"). If clean, run
   `scripts/reset-non-passed.sh` and re-run pass 1 to convert the
   17 `failed-needs-iter14-sd-*` scenarios.

2. **VM SSH lockout.** Around 03:18 local the VM started rejecting
   public-key auth (`Permission denied (publickey,password,
   keyboard-interactive)`) — most likely sshd's `MaxAuthTries`
   triggered after the back-to-back pipeline runs. ICMP also blocked
   but TCP/22 reachable; auth alone is the failure mode. Should
   self-clear after sshd's `LoginGraceTime` cooldown (default 120s
   per attempt × 6 retries) or a manual sshd restart.

3. **Pre-existing GPL-tooling reference in `src/mkfs.rs:13–15`.** The
   module-level docstring cites Flatcap URLs as references — this
   violates the project's hard rule (CLAUDE.md / project memory) on
   GPL-tooling name-drops. Did **not** clean up under "one fix per
   iteration"; flagged here for a separate follow-up commit.

4. **Operation-host-table mismatch with iter13's reality.** The
   protocol's "Operation host requirements" table marks `Mac: write`
   and `Mac: delete` as NOT YET. They're actually plumbed at the
   `Filesystem::{create_file, unlink, mkdir, rmdir}` level — the new
   `inspect_ntfs` binary exposes them. The "blocked-on-mac-writer/
   -deleter" scenarios should be re-classified after the iter14 SD
   fix passes chkdsk.

5. **Single agent run.** The protocol assumes N concurrent agents
   sharing the work-list. This run had only one. The
   `claim-scenario.sh` race logic is therefore untested in anger;
   future multi-agent runs will exercise it.

## Commits on this branch

```
7984f94 scripts/reset-non-passed.sh: pass-2/3 helper
9b06ca4 findings iter14 + 1 mac-only scenario passed
3144024 iter14 + inspect_ntfs CLI (UNVERIFIED — VM offline mid-run)
a7ae58f matrix pass 1: classify all pending scenarios under iter13's writer
1c5007a mac-format-basic-256mib: populate root $I30 with system-file entries
```

Branch is `agent/agent-c5fe-2026-05-02`, never pushed to `origin/main`
per protocol.
