# agent-c6a1-2026-05-02

- Session start: 2026-05-02 ~02:43 PDT
- Session end:   2026-05-02 ~03:30 PDT (~50 min wall)
- Branch: `agent/agent-c6a1-2026-05-02`
- Worktree: `.claude/worktrees/agent-c6a1-2026-05-02/`

## Scenarios touched

| Scenario | Final status | Notes |
|---|---|---|
| `mac-format-label-cjk` | `passed-implicitly-by-agent-5442-2026-05-02-c6a1` | iter13 fix from 5442 verified end-to-end on this scenario; CJK label survives `mkfs_ntfs` UTF-16 encoding intact |
| `mac-format-basic-256mib` | `blocked-on-agent-5442-2026-05-02` | initially claimed in error (work-list races between worktrees); released after seeing 5442 was already on it |
| `mac-format-tiny-32mib` | `pending` (released) | claimed to verify 840e's iter14 BPB fix; SSH to VM died before pipeline could run; released for next agent |
| `mac-format-{mac-enumerate-empty,win-write-mac-enumerate,win-write-win-delete-mac-verify-empty}` | `blocked-needs-scaffolding-mac-enumerate-cli-agent-c6a1-2026-05-02` | unblocked by my `inspect_ntfs` CLI in `fb54444`; future agent should reset these to pending and run them |
| `win-format-win-write-mac-verify` | `blocked-needs-scaffolding-mac-enumerate-cli-agent-c6a1-2026-05-02` | same — unblocked by inspect_ntfs |
| `win-format-win-write-mac-write-win-verify` | `blocked-needs-scaffolding-mac-writer-agent-c6a1-2026-05-02` | facade has `create_file`/`write_file_contents` already; needs ~80-line CLI wrapper |
| `win-format-win-write-mac-delete-win-verify` | `blocked-needs-scaffolding-mac-deleter-agent-c6a1-2026-05-02` | facade has `unlink`/`rmdir` already; needs ~50-line CLI wrapper |
| `mac-format-win-write-{tiny,medium,large,many-small,then-delete}` | `blocked-needs-runner-winfixtures-wiring-agent-c6a1-2026-05-02` | runner has `-WinFixtures` param but body unwired; needs PowerShell write block after mount |
| (still pending) | `mac-format-small-64mib`, `mac-format-large-1gib`, `mac-format-cluster-{512,1k,8k,64k}`, `mac-format-label-{empty,32chars,latin1}`, `mac-format-tiny-32mib` | runnable when VM SSH recovers |

Final tally: **12 blocked, 10 pending, 1 passed (mine)**, plus passed/failed claims by other sessions on their own branches.

## What worked

### Pipeline iter1 against `mac-format-label-cjk` (256 MiB, CJK label)

Surfaced exactly the same root cause as `mac-format-basic-256mib` (which 5442 was independently working on): every system MFT record reported as orphaned in chkdsk Stage 2.

**Per-field diff** (root rec 5 `INDEX_ROOT '$I30'`, my pre-fix diag dir `iter-20260502-025140`):

| Field | reference | ours |
|---|---|---|
| `INDEX_HEADER.idx_len` | 1112 | 32 |
| entries (excluding LAST sentinel) | 11 | 0 |

Reference's 11 entries: `$AttrDef`, `$BadClus`, `$Bitmap`, `$Boot`, `$LogFile`, `$MFT`, `$MFTMirr`, `$Quota`, `$UpCase`, `$Volume`, `.` (sorted COLLATION_FILE_NAME).

### Pipeline iter2 (post-fix) — independent corroboration

After cherry-picking 5442's `f3ea014` fix, the same scenario re-ran to the same residual state as 5442's diag dir on basic-256mib: **all 12 orphan-recovery messages gone**, only the trailing `frs.cxx 0x60f` internal chkdsk error remains.

This is significant — two independently chosen scenarios reach byte-identical post-fix states. The fix is not specific to volume parameters.

### CJK label encoding verified byte-perfect

Decoded `$Volume`'s `$VOLUME_NAME` from `iter-20260502-030838/ours-mft-16recs.bin` (rec 3): exactly `E5 65 2C 67 9E 8A E9 30 D9 30 EB 30` = `日本語ラベル` in UTF-16-LE. chkdsk's stdout rendering as `??????` is its own console-codepage limitation (chkdsk pipes to a non-UTF-aware stream); the bytes on disk are correct. No writer bug here — just a console encoding artifact.

## Iteration entries added

- `iter13b` in `docs/chkdsk-findings.md`: corroboration of 5442's iter13 on `mac-format-label-cjk`. Plus diag dir paths for both pre- and post-fix.
- `iter14` in `docs/chkdsk-findings.md`: 840e's BPB NumberSectors fix (cherry-picked from `41e601e` and renumbered from their "iter13" to dodge the conflict with 5442's already-shipped iter13).

## Code changes

- `src/bin/inspect_ntfs.rs` — new ~120 line read-only enumerate CLI (commit `fb54444`). Wraps `Filesystem::read_dir`, depth-first walker, sorted output, `-t` for type prefix. Smoke-tested against a fresh 256 MiB volume; correctly lists all 11 system files in collation order.
- `Cargo.toml` — `[[bin]] inspect_ntfs` registered.
- Cherry-picks (no original code change): 5442's `f3ea014` (iter13 root $I30 populate fix) and 840e's `41e601e` (iter14 BPB NumberSectors fix) and `48fb998` (runner -ClusterSize plumbing).

## Linux test contract

`cargo test --release --lib mkfs --test mkfs_roundtrip --test mkfs_bin_smoke` — 6/6 pass before, 6/6 pass after. `cargo fmt --check` clean. `cargo clippy --all-targets -- -D warnings` clean. Pre-commit hook passes on every commit.

## Infrastructure issue — same VM-SSH lockout 8934 reported

VM SSH worked at session start (the user-requested smoke test was clean) and for one full pipeline iteration (`iter-20260502-025140` = pre-fix on label-cjk). Then the second pipeline run (`iter-20260502-030838` = post-fix on label-cjk) succeeded — that one's diag is the proof of iter13 working.

After that, every SSH attempt has returned `Permission denied (publickey,password,keyboard-interactive)` despite the same agent-loaded ED25519 key (`SHA256:e0qHA9BShBoP3t16PSwY6HoJtUJXkWif1rXX+64WV4w` / `chris.thomas@semdatex.com`). I retried over ~5 minutes with backoff (one Monitor block of 8 retries, then a 5-minute Monitor block); no recovery.

Per protocol I did NOT attempt to ssh-fix the VM. Same lockout 8934 reported (their report at `tests/matrix/agent-reports/agent-8934-2026-05-02.md` on `worktree-agent-8934-2026-05-02`).

**Most likely cause** (matches 8934's hypothesis): Windows OpenSSH's failed-attempt lockout triggered by 5+ concurrent agent worktrees racing for SSH connections, OR `authorized_keys` was modified by a sibling agent. Either way it's the morning's first thing to check.

## What's left for the morning review

1. **Restore VM SSH access.** Until that, no agent can advance the 10 still-pending VM-dependent scenarios.
2. **Reset every `blocked-*` scenario in `tests/matrix/work-list.json` to `pending`** once the WinFixtures runner block lands and the writer/deleter CLIs are scaffolded — the `blocked-needs-scaffolding-*` markers will be stale by then.
3. **iter15 (next mkfs bug)**: 8934's report identifies the leading candidate — every Microsoft system record carries a `$SECURITY_DESCRIPTOR` attribute (type 0x50) that we don't write. The trailing `frs.cxx 0x60f` chkdsk error and the persistent NTFS Event ID 55 are the symptoms. Also `$Extend` rec 11 differs in structure between reference (a regular file with `$DATA`) and ours (a directory with `$INDEX_ROOT:$I30`).
4. **Several agents independently produced the same iter13 fix** (5442 in `f3ea014`, 8a29 in `6e203b9`, c5fe in `1c5007a`, 8934 in `7e87e87`). Pick the cleanest one — 5442's was the earliest landed and has the most thorough commit message — and merge it to main as the canonical iter13. The duplicate work is the cost of running parallel sessions without an integration branch.
5. **My branch `agent/agent-c6a1-2026-05-02`** is ready to merge. Contents in dependency order: 8934's runner env-var infra, 5442's iter13 fix + findings, my iter13b corroboration, 840e's iter14 BPB fix + ClusterSize runner, my work-list status sweep, my `inspect_ntfs` CLI.

## Commits on my branch (chronological, beyond `main`)

```
fb54444 infra(test): add inspect_ntfs CLI (read-only enumerate)
0c5c2a7 work-list: mark scaffolding-dependent scenarios as blocked
4bef294 runner: pass -ClusterSize through to mkfs_ntfs           [picked from 840e]
84a83d7 Reapply "mac-format-tiny-32mib: BPB NumberSectors = ..." [picked from 840e]
ca23d33 docs(findings): iter13b — label-cjk corroborates iter13 fix
d4bdec9 docs(findings): iter13 entry — root $I30 populate fix    [picked from 5442]
2325f7b fix(mkfs): populate root $I30 with INDEX_ENTRY ...        [picked from 5442]
1fd0969 mac-format-label-cjk: claim scenario for agent-c6a1-2026-05-02
aa366ea infra(test): plumb scenario params through test-windows-local.sh [picked from 8934]
```

(Plus this report and a final work-list status update commit.)
